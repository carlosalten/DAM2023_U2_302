import 'package:flutter/material.dart';

class TabEquipo extends StatelessWidget {
  const TabEquipo({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Equipo', style: TextStyle(fontSize: 20)),
      ),
    );
  }
}
