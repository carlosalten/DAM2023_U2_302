import 'package:flutter/material.dart';

class CircuitosPage extends StatelessWidget {
  const CircuitosPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Circuitos',
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}
