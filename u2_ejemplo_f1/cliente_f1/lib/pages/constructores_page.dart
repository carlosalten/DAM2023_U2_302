import 'package:flutter/material.dart';

class ConstructoresPage extends StatelessWidget {
  const ConstructoresPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Constructores',
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}
